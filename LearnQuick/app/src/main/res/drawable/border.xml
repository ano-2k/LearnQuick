<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color of the border -->
    <stroke
        android:color="@android:color/darker_gray"
    android:width="3dp"/> <!-- Increase border thickness to make it more visible -->
    <corners android:radius="10dp"/> <!-- More rounded corners for a smoother look -->
</shape>